<p id="notice"><%= notice %></p>

<h1>Listing Ads</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Description</th>
     <!--  <th>Contact</th>
      <th>College</th>
      <th>Branch</th>
      <th>Semester</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ads.each do |ad| %>
      <tr>
        <td><%= User.find_by_id(ad.user_id).username %></td>
        <td><%= ad.title %></td>
        <td><%= ad.description %></td>
        <!-- <td><%= ad.contact %></td>
        <td><%= ad.college %></td>
        <td><%= ad.branch %></td>
        <td><%= ad.semester %></td> -->
        <td><%= link_to 'Show', ad %></td>
<!--         <td><%= link_to 'Edit', edit_ad_path(ad) %></td>
        <td><%= link_to 'Destroy', ad, method: :delete, data: { confirm: 'Are you sure?' } %></td>
 -->      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ad', new_ad_path %>
